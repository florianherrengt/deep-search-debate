use_default_settings: true

general:
  instance_name: Deep Search Debate

search:
  formats:
    - html
    - json

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: local-development-only-searxng-secret
  limiter: false
  image_proxy: false
